function Message() { // constructor for background / schloo


	game.ctx.save();
	ctx.fillStyle = "rgb(0, 0, 255)";
	ctx.globalAlpha = 0.5;
	ctx.fillRect(100, 100, 700, 400);
	game.ctx.restore();
	
}
